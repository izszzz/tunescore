import Toolbar from "@mui/material/Toolbar";
import React from "react";
import Header from ".";

const EditorHeader = () => {
	return (
		<>
			<Header>
				editor
			</Header>
			<Toolbar />
		</>
	);
};

export default EditorHeader;