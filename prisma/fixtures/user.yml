entity: user
items:
  user:
    name: user
    email: "{{internet.email}}"

  user_long_name:
    name: <%= "あ".repeat(100) %>
    email: "{{internet.email}}"

  followed_user:
    name: followed
    email: "{{internet.email}}"

  following_user:
    name: following
    email: "{{internet.email}}"
